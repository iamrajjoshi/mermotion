{
  "name": "@mermotion/cli",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "bin": {
    "mermotion": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.node.config.ts && vite build --config vite.config.ts",
    "test:render": "pnpm --filter @mermotion/engine build && pnpm run build && node dist/index.js setup --json && MERMOTION_BROWSER_TEST=1 vitest run src/render.integration.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.browser.json --noEmit"
  },
  "dependencies": {
    "@mermotion/engine": "workspace:*",
    "commander": "catalog:",
    "gifenc": "catalog:",
    "playwright": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
